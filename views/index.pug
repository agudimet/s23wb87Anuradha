extends layout

block content
  h1= title
  p Welcome to #{title}
  p Hello Anuradha Gudimetla.
  //-button(type="button", class="btn btn-secondary")Press Me
  //-button.btn.btn-primary Press Me  
  button.btn.btn-primary(type='button', onclick='doReduce()') Press Me
  div#reduce Reduce the value
  script.
      let count = 20;
      let change = -1;
      function doReduce() {
         count += change;
         change--;
      document.querySelector("#reduce").textContent = `Count ${count} with change ${change}`;
     }
  a(href="/books") IT data
  